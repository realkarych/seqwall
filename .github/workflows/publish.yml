name: Publish Go Release

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'SemVer tag to release (e.g. v0.0.8)'
        required: true

permissions:
  contents: write
  packages: write

jobs:
  resolve-release:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.release.outputs.TAG }}
      sha: ${{ steps.release.outputs.SHA }}

    steps:
      - name: Checkout release refs
        uses: actions/checkout@v6.0.2
        with:
          fetch-depth: 0

      - name: Resolve release tag
        id: release
        env:
          INPUT_TAG: ${{ github.event.inputs.tag }}
        run: |
          if [ "$GITHUB_EVENT_NAME" = "workflow_dispatch" ]; then
            TAG="$INPUT_TAG"
          else
            TAG="${GITHUB_REF#refs/tags/}"
          fi
          git check-ref-format "refs/tags/$TAG"
          SHA=$(git rev-parse --verify "refs/tags/${TAG}^{commit}")
          printf 'TAG=%s\nSHA=%s\n' "$TAG" "$SHA" >>"$GITHUB_OUTPUT"

  build-and-publish:
    needs: resolve-release
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ needs.resolve-release.outputs.tag }}
    strategy:
      matrix:
        goos: [linux, darwin, windows]
        goarch: [amd64, arm64]

    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.2
        with:
          ref: ${{ needs.resolve-release.outputs.sha }}

      - name: Set up Go
        uses: actions/setup-go@v6.4.0
        with:
          go-version: '1.24'

      - name: Build binary
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: 0
          VERSION: ${{ needs.resolve-release.outputs.tag }}
        run: |
          mkdir -p dist
          BIN="seqwall"
          [ "$GOOS" = "windows" ] && BIN="${BIN}.exe"
          go build -ldflags="-s -w -X 'main.Version=${VERSION}'" -o "dist/${BIN}" .


      - name: Archive binary
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          TAG: ${{ needs.resolve-release.outputs.tag }}
        run: |
          cd dist
          ARCHIVE_PREFIX="${TAG}_seqwall_${GOOS}_${GOARCH}"
          if [ "$GOOS" = "windows" ]; then
            zip "${ARCHIVE_PREFIX}.zip" "seqwall.exe"
          else
            tar -czf "${ARCHIVE_PREFIX}.tar.gz" "seqwall"
          fi

      - name: Upload to GitHub Releases
        uses: softprops/action-gh-release@v3.0.0
        with:
          tag_name: ${{ needs.resolve-release.outputs.tag }}
          name: ${{ needs.resolve-release.outputs.tag }}
          files: |
            dist/*.tar.gz
            dist/*.zip
          fail_on_unmatched_files: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  update-homebrew:
    needs: build-and-publish
    runs-on: ubuntu-latest
    if: |
      (github.event_name == 'workflow_dispatch' && github.event.inputs.tag != '') ||
      startsWith(github.ref, 'refs/tags/')

    env:
      TAG: ${{ needs.build-and-publish.outputs.tag }}
      TAP_REPO: realkarych/homebrew-tap
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - name: Get tap default branch
        id: tapinfo
        run: |
          branch=$(gh repo view "$TAP_REPO" --json defaultBranchRef -q .defaultBranchRef.name)
          echo "BRANCH=$branch" >>"$GITHUB_OUTPUT"

      - name: Checkout tap
        uses: actions/checkout@v6.0.2
        with:
          repository: ${{ env.TAP_REPO }}
          token: ${{ secrets.HOMEBREW_TOKEN }}
          path: tap
          ref: ${{ steps.tapinfo.outputs.BRANCH }}

      - name: Download Homebrew archives
        run: |
          mkdir -p dl
          gh release download "$TAG" -R realkarych/seqwall -D dl -p "${TAG}_seqwall_darwin_amd64.tar.gz"
          gh release download "$TAG" -R realkarych/seqwall -D dl -p "${TAG}_seqwall_darwin_arm64.tar.gz"
          gh release download "$TAG" -R realkarych/seqwall -D dl -p "${TAG}_seqwall_linux_amd64.tar.gz"
          gh release download "$TAG" -R realkarych/seqwall -D dl -p "${TAG}_seqwall_linux_arm64.tar.gz"

      - name: Calculate SHA256
        id: shas
        run: |
          DARWIN_AMD64=$(sha256sum dl/*darwin_amd64.tar.gz | cut -d' ' -f1)
          DARWIN_ARM64=$(sha256sum dl/*darwin_arm64.tar.gz | cut -d' ' -f1)
          LINUX_AMD64=$(sha256sum dl/*linux_amd64.tar.gz | cut -d' ' -f1)
          LINUX_ARM64=$(sha256sum dl/*linux_arm64.tar.gz | cut -d' ' -f1)
          {
            echo "DARWIN_AMD64=$DARWIN_AMD64"
            echo "DARWIN_ARM64=$DARWIN_ARM64"
            echo "LINUX_AMD64=$LINUX_AMD64"
            echo "LINUX_ARM64=$LINUX_ARM64"
          } >>"$GITHUB_OUTPUT"

      - name: Update formula
        run: |
          mkdir -p tap/Formula
          cat > tap/Formula/seqwall.rb <<EOF
          class Seqwall < Formula
            desc "Testing tool for PostgreSQL migrations"
            homepage "https://github.com/realkarych/seqwall"

            on_macos do
              if Hardware::CPU.arm?
                url "https://github.com/realkarych/seqwall/releases/download/${TAG}/${TAG}_seqwall_darwin_arm64.tar.gz"
                sha256 "${{ steps.shas.outputs.DARWIN_ARM64 }}"
              else
                url "https://github.com/realkarych/seqwall/releases/download/${TAG}/${TAG}_seqwall_darwin_amd64.tar.gz"
                sha256 "${{ steps.shas.outputs.DARWIN_AMD64 }}"
              end
            end

            on_linux do
              if Hardware::CPU.arm?
                url "https://github.com/realkarych/seqwall/releases/download/${TAG}/${TAG}_seqwall_linux_arm64.tar.gz"
                sha256 "${{ steps.shas.outputs.LINUX_ARM64 }}"
              else
                url "https://github.com/realkarych/seqwall/releases/download/${TAG}/${TAG}_seqwall_linux_amd64.tar.gz"
                sha256 "${{ steps.shas.outputs.LINUX_AMD64 }}"
              end
            end

            def install
              bin.install "seqwall"
            end

            test do
              system bin/"seqwall", "--version"
            end
          end
          EOF

      - name: Commit and push formula
        run: |
          cd tap
          git config user.name github-actions
          git config user.email noreply@github.com
          git add Formula/seqwall.rb
          git commit -m "seqwall ${TAG}" || echo "Nothing to commit"
          git push origin HEAD:${{ steps.tapinfo.outputs.BRANCH }}

  update-apt:
    needs: build-and-publish
    runs-on: ubuntu-latest
    if: |
      (github.event_name == 'workflow_dispatch' && github.event.inputs.tag != '') ||
      startsWith(github.ref, 'refs/tags/')

    env:
      TAG: ${{ needs.build-and-publish.outputs.tag }}
      VER: ${{ needs.build-and-publish.outputs.tag }}
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - name: Install tools
        run: |
          sudo apt-get update
          sudo apt-get install -y ruby ruby-dev gnupg2 aptly rsync
          sudo gem install --no-document fpm

      - name: Download linux artifacts
        run: |
          mkdir -p dl
          gh release download "$TAG" -R realkarych/seqwall \
            -D dl -p "${TAG}_seqwall_linux_*.tar.gz"

      - name: Extract binaries
        run: |
          mkdir -p deb-src
          for tgz in dl/*.tar.gz; do
            arch=$(echo "$tgz" | sed -E 's/.*_linux_([a-z0-9]+)\.tar\.gz/\1/')
            tar -xOzf "$tgz" > "deb-src/seqwall_${arch}"
            chmod +x "deb-src/seqwall_${arch}"
          done

      - name: Build .deb packages
        run: |
          mkdir -p deb
          cd deb-src
          for bin in seqwall_*; do
            arch=${bin##*_}
            fpm -s dir -t deb \
                -p "../deb/seqwall_${VER#v}_${arch}.deb" \
                --name seqwall \
                --version "${VER#v}" \
                --architecture "$arch" \
                --description "Testing tool for PostgreSQL migrations" \
                --license MIT \
                --url "https://github.com/realkarych/seqwall" \
                "$bin=/usr/bin/seqwall"
          done

      - name: Import GPG
        env:
          GPG_KEY: ${{ secrets.APT_GPG_PRIVATE_KEY }}
        run: echo "$GPG_KEY" | gpg --batch --import

      - name: Checkout APT pages
        uses: actions/checkout@v6.0.2
        with:
          repository: realkarych/seqwall-apt
          token: ${{ secrets.DEB_TOKEN }}
          path: apt
          ref: gh-pages

      - name: Publish repo
        run: |
          cd apt
          aptly repo create -distribution=stable -component=main seqwall || true
          aptly repo add seqwall ../deb/*.deb
          KEYID=$(gpg --list-keys --with-colons | awk -F: '/^pub/ {print $5; exit}')
          aptly publish repo -architectures="amd64,arm64" -gpg-key="$KEYID" seqwall
          cp -r ~/.aptly/public/* .
          rm -rf ~/.aptly

      - name: Commit & push APT repo
        run: |
          cd apt
          git config user.name github-actions
          git config user.email noreply@github.com
          git add .
          git commit -m "APT release ${TAG}" || echo "no changes"
          git push origin HEAD:gh-pages

  docker-image:
    needs: [resolve-release, build-and-publish]
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2
        with:
          ref: ${{ needs.resolve-release.outputs.sha }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4.0.0

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v4.1.0

      - name: Login to GHCR
        uses: docker/login-action@v4.2.0
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push image
        uses: docker/build-push-action@v7.3.0
        with:
          context: .
          file: ./Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          cache-from: type=gha
          cache-to:   type=gha,mode=max
          build-args: |
            VERSION=${{ needs.build-and-publish.outputs.tag }}
          labels: |
            org.opencontainers.image.source=https://github.com/realkarych/seqwall
          tags: |
            ghcr.io/realkarych/seqwall:${{ needs.build-and-publish.outputs.tag }}
            ghcr.io/realkarych/seqwall:latest

      - name: Smoke-test image
        env:
          TAG: ${{ needs.build-and-publish.outputs.tag }}
        run: |
          docker run --rm \
            "ghcr.io/realkarych/seqwall:${TAG}" \
            --version
